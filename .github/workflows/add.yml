name: Add Reviewer
on:
  pull_request:
    types: [review_requested]

concurrency:
  group: reviewer-weights-lock
  cancel-in-progress: false

jobs:
  add-reviewer:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: ./
